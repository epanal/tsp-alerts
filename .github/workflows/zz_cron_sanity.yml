name: Cron Sanity (UI)

on:
  schedule:
    - cron: "*/2 * * * *"  # every 2 minutes, UTC
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "UTC now:" && date -u
          echo "Ref:  $GITHUB_REF"
          echo "SHA:  $GITHUB_SHA"
